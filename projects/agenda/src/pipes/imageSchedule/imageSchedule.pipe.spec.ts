import { ImageSchedulePipe } from './imageSchedule.pipe';

describe('ImageSchedulePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSchedulePipe();
    expect(pipe).toBeTruthy();
  });
});
